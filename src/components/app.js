import React from 'react';
import '../styles/app.css';

export default function app() {
    return (
        <div>
            <h1>Basic React Config</h1>
            <span>A basic react project setup with Webpack, Babel and Express!</span>
        </div>
    )
};